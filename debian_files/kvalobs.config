#!/bin/sh

#export DEBCONF_DEBUG=developer
#export dbc_debug=1

. /usr/share/debconf/confmodule
db_version 2.0

dbc_first_version=2.1.1

if [ -f /usr/share/dbconfig-common/dpkg/config.pgsql ]; then
   . /usr/share/dbconfig-common/dpkg/config.pgsql 
   dbc_go kvalobs $@
fi
