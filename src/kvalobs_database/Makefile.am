pkglibdir = $(libdir)/@PACKAGE_NAME@@KVALOBSLIBS_BIN_VERSION@
sqldir = $(pkgdatadir)/db

sql_DATA = kvalobs_roles.sql kvalobs_schema.sql kvalobs_createdb.sql cleanup_kvalobsdb.sql

EXTRA_DIST = $(sql_DATA)

SUBDIRS = upgrade